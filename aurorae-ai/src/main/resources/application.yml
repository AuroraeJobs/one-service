spring:
  application:
    name: aurorae-ai
  
# DeepSeek Configuration
deepseek:
  api-key: ${DEEPSEEK_API_KEY:your-api-key}
  base-url: https://api.deepseek.com/v1
  model: deepseek-chat

server:
  port: 8088
  servlet:
    context-path: /ai