spring:
  boot:
    admin:
      context-path: ${MONITOR_CONTEXT_PATH:/}
      ui:
        title: Admin Monitor
        public-url: ${MONITOR_PUBLIC_URL:/}
        brand: <span>Admin Monitor</span>
  devtools:
    livereload:
      port: 35730
