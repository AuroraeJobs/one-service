spring:
  security:
    user:
      name: ${SECURITY_USER_NAME:aurorae}
      password: ${SECURITY_USER_PASSWORD:jobs}

eureka:
  instance:
    prefer-ip-address: true
    hostname: @project.artifactId@
    status-page-url-path: /actuator/info
    health-check-url-path: /actuator/health
    lease-renewal-interval-in-seconds: 10
    lease-expiration-duration-in-seconds: 15
  client:
    region: shanghai
    availability-zones:
      shanghai: defaultZone
    prefer-same-zone-eureka: true
    registry-fetch-interval-seconds: 5
    service-url:
      defaultZone: http://${spring.security.user.name}:${spring.security.user.password}@${EUREKA_HOST:localhost}:${EUREKA_PORT:8100}/eureka/