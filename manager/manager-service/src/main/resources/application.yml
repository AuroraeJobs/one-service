server:
  port: 8081
spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: test
  dubbo:
    application:
      name: manager-service
      registry: zookeeper://127.0.0.1:2181
